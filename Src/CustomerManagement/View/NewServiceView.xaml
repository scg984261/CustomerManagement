<UserControl x:Class="CustomerManagement.View.NewServiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomerManagement.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <!-- Title -->
            <RowDefinition Height="50" />

            <!-- Input Form -->
            <RowDefinition />

            <!-- Bottom panel - buttons -->
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="New Service" Style="{StaticResource DetailsHeader}" />
        
        <!-- Service Details Input Form -->
        <Grid Grid.Row="1"  Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="250" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0">Name</Label>
            <Label Grid.Row="1" Grid.Column="0">Price</Label>
            <Label Grid.Row="2" Grid.Column="0">Price</Label>
            <Label Grid.Row="3" Grid.Column="0">Recurring</Label>

            <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource FormTextBox}" Text="{Binding Name,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FormTextBox}" Text="{Binding PriceString,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="2" Grid.Column="1" Style="{StaticResource TabTextBlock}" Text="{Binding PriceFormatted,Mode=OneWay}" />
            <Viewbox Grid.Row="3" Grid.Column="1">
                <CheckBox IsChecked="{Binding IsRecurring, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Viewbox>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!-- Cancel button -->
            <Button Grid.Row="0" Grid.Column="0" Command="{Binding NavigateBackDelegateCommand}">
                <Image Source="/Images/BackArrow.png" />
            </Button>

            <!-- Save button -->
            <Button Grid.Row="0" Grid.Column="1" Content="Save" Command="{Binding SaveServiceCommand}" />
        </Grid>
    </Grid>
</UserControl>
