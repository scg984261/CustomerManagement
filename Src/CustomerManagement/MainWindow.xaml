<Window x:Class="CustomerManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:userControls="clr-namespace:CustomerManagement.View.UserControls"
        xmlns:local="clr-namespace:CustomerManagement"
        WindowStartupLocation="CenterScreen"
        Title="CMS" Height="900" Width="1500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <userControls:MenuBar />

        <TabControl Name="TabCollection" Grid.Row="1">
            <TabItem  Header="Main Details">
                <userControls:CustomerDataGrid Grid.Row="1" />
            </TabItem>

            <TabItem Header="Tab 2" Content="Hello, World" />
        </TabControl>

        <userControls:FooterBar Grid.Row="3" />
    </Grid>
</Window>
